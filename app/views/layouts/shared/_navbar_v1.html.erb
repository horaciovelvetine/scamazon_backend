
<div class="container-fluid">
  <nav class="navbar navbar-expand navbar-dark bg-dark fixed-top navbar-nav">
      <div class=" d-flex flex-row align-items-center">
        <div class="col-1">
          <%= link_to image_tag("main-logo", alt:"Scamazon Logo", class:"img-fluid"), root_path %>
        </div>
        
        <div class="flex-col flex-fill">
          <%= render "/search/shared/flex_form" %>
        </div>
        
        <div class="flex-col">
          <% if !@user %>
            <%= render "/layouts/navbar_components/no_user_dropdown" %>
          <% else %>
            <%= render "/layouts/navbar_components/user_dropdown" %>
          <% end %>
        </div>

        <div class="flex-col">
          <%= render "/shopping_cart/shared/navbar_display"%>
        </div>
      </div>
  </nav>
</div>

