<div class="card">
  <div class="card-body">
    <div class="row">
      <strong><h4 class="card-title">Your Current Balance:<span class="text-success"> $<%= @user.balance %></strong></span></h4>
    </div>
    <%= render 'form', gift_card: @gift_card %>
    
  </div>
</div>
